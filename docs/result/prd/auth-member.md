# PRD — 회원 · 인증 (auth-member)

> 담당: **A (최동훈)**
> 상위 사양: `docs/result/spec-fixed.md` §2, §11.3
> 상태: **2-1 뼈대 완료 / 기술 결정 TODO**

---

## 1. 개요

### 목적
서비스의 모든 행위 주체인 `User`를 만들고, 그 계정의 생명주기(가입 → 사용 → 비활성화 → 재활성화 또는 파기)를 관리한다.

### 왜 가장 먼저인가
다른 모든 도메인(공고·신청·포인트·제재·평점)이 `User`를 FK로 참조한다. 이 스키마가 흔들리면 전부 흔들린다.

### 범위
- 이메일 인증 기반 가입
- JWT 인증 및 보호 라우팅
- 주소 등록 (카카오 우편번호 + 좌표)
- 비밀번호 재설정 (변경은 차단)
- 탈퇴(비활성화) / 재활성화 / 개인정보 파기
- 관리자 회원 목록·상세 화면

### 범위 밖
전자서명·동의서는 `agreement` PRD가 다룬다. 가입 플로우의 5번째 단계로 호출만 한다.

---

## 2. 사용자 스토리

### 가입
- **구직자로서** 이메일 인증을 거쳐 가입하고 싶다. 실제로 받을 수 있는 주소여야 재동의 요청·제재 알림이 도달하기 때문이다.
- **가입자로서** 주소를 직접 타이핑하지 않고 검색해서 넣고 싶다. 오타가 있으면 근무지가 엉뚱한 곳으로 잡힌다.

### 인증
- **회원으로서** 로그아웃한 뒤 뒤로가기를 눌러도 내 정보가 보이지 않기를 원한다. 공용 PC에서 쓸 수 있기 때문이다.
- **회원으로서** 비밀번호를 잊어도 계정을 되찾고 싶다. 포인트 잔고가 묶여 있기 때문이다.

### 계정 종료
- **탈퇴하려는 회원으로서** 포인트가 남아 있으면 탈퇴가 막히기를 원한다. 실수로 돈을 잃고 싶지 않다.
- **탈퇴하려는 회원으로서** 진행 중인 일이 있으면 탈퇴가 막히기를 원한다 — 반대로 **구인자로서는**, 수락한 사람이 탈퇴로 사라지지 않기를 원한다.
- **돌아온 사용자로서** 예전 계정이 살아나기를 원한다. 쌓아둔 평점을 잃고 싶지 않다.

### 관리자
- **관리자로서** 이름·주소로 회원을 검색해 상세를 열고, 평점·거래 이력·제재 이력을 한 화면에서 보고 싶다. 분쟁 문의가 오면 그 사람의 이력을 즉시 확인해야 한다.

---

## 3. 기술 결정 (ADR)

> **TODO** — 2-2 단계에서 각 항목마다 3안을 비교하고 사용자가 선택한 뒤 ADR 4요소로 채운다.

| ID | 결정할 것 | 왜 3안 비교가 필요한가 |
|---|---|---|
| `ADR-AUTH-1` | 인증 토큰의 저장·갱신·무효화 구조 | Refresh 토큰을 DB에 어떤 단위로 둘지(계정당 1개 / 세션당 1개 / 회전)에 따라 다중 기기 로그인과 강제 로그아웃 동작이 달라진다 |
| `ADR-AUTH-2` | 주소 데이터 저장 구조 | 관리자 화면이 **시/도 → 시/군/구 필터**를 요구한다. 단일 문자열로 저장하면 필터를 못 걸고, 분해해서 저장하면 카카오 응답 형식에 종속된다 |
| `ADR-AUTH-3` | 계정 생명주기 상태 표현 | 활성 / 비활성화 / 파기됨 3상태 + 이메일 유니크 제약 + 재활성화가 서로 얽힌다. 플래그 조합·상태 컬럼·별도 테이블 중 무엇으로 표현할지 |
| `ADR-AUTH-4` | 이메일 인증 코드 저장·검증 구조 | 만료 10분·재발송 쿨다운 60초·시간당 5회 제한을 어디서 판정할지 (DB / 메모리 / 별도 테이블) |

---

## 4. Out of Scope

이번에 **만들지 않는다.** 확장 해석 금지.

| 항목 | 이유 |
|---|---|
| 소셜 로그인 (카카오·구글·네이버) | 로컬 가입 전제 (planner.md). PDF 동의서 서명이 가입 플로우에 묶여 있어 소셜과 궁합이 나쁘다 |
| 휴대폰 본인인증 | 사업자등록 필요 |
| 관리자 2단계 인증 | MVP 이후 (spec-fixed §12.1) |
| 마이페이지 비밀번호 변경 UI | **의도적으로 차단** (spec-fixed §2.4). 재설정만 허용 |
| 아이디 찾기 | 이메일이 곧 아이디라 개념 자체가 없다 |
| 프로필 이미지 업로드 | 별점만 쓰기로 합의된 서비스라 신원 표현이 불필요 |
| 다국어 / i18n | 국내 서비스 |
| 회원 등급·멤버십 | 없음 |
| 관리자 화면에서 회원 정보 직접 수정 | 조회·제재 해제만. 개인정보 임의 수정은 사고 위험 |
| 로그인 이력 / 접속 기기 관리 화면 | Refresh 토큰은 DB에 있지만 사용자에게 노출하지 않는다 |

---

## 5. 용어 정의

> `spec-fixed.md` §0에서 동기화. 이 PRD에서 새로 만드는 용어는 없다.

| 용어 | 코드 식별자 | 정의 |
|---|---|---|
| 회원 | `User` | 서비스 가입자. 역할 구분 없는 단일 엔티티 |
| 구인자 | `poster` | 공고를 등록한 `User` (관계상의 역할) |
| 구직자 | `worker` | 공고에 신청한 `User` (관계상의 역할) |
| 비활성화 | `deactivatedAt` | 탈퇴한 회원. 소프트 삭제 상태. 로그인 불가 |
| 관리자 | `role = ADMIN` | 별도 테이블 아님 |

**금지어:** "탈퇴 회원 / 삭제 회원" → **비활성화 회원**

---

## 6. 연결된 도메인

| 도메인 | 관계 |
|---|---|
| `agreement` | 가입 플로우 5단계에서 동의서 생성을 호출 |
| `point-money` | 탈퇴 보류 조건에 잔액 조회 필요 |
| `job-post` / `application` | 탈퇴 보류 조건에 진행 중 계약 조회 필요 |
| `penalty-rating` | 회원 상세 화면이 평점·리뷰·제재 이력을 읽음 |
