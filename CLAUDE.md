# fixer

동네 일거리 중개 서비스. **동시에 이 저장소는 AI 개발 하네스(harness, AI에게 일을 맡기기 위한 규칙·스킬·훅 묶음)를 실험하는 곳이다.**

---

## 이 프로젝트에서 AI에게 허용하는 것

**AI는 `src`를 포함한 구현 코드를 사전 확인 없이 작성·수정해도 된다.**

일반적으로는 코드를 쓰기 전에 사람에게 물어보는 것이 맞다. 하지만 이 저장소의 목적 자체가 *AI가 어디까지 스스로 만들 수 있는가*를 시험하는 것이므로, 매번 허가를 받으면 실험이 되지 않는다.

이 허용은 **이 저장소에만** 적용된다. 다른 프로젝트로 이 규칙을 옮기지 않는다.

### 그래도 여전히 확인을 받아야 하는 것

허용된 것은 "코드 작성"이지 "모든 행동"이 아니다. 아래는 되돌리기 어렵거나 저장소 밖으로 나가는 일이라 그대로 확인을 받는다.

| 행동                                  | 이유                          |
| ------------------------------------- | ----------------------------- |
| `git push`                            | 되돌리려면 이력을 고쳐야 한다 |
| GitHub 이슈·PR 생성, 라벨 변경        | 저장소 밖에 만들어진다        |
| 외부 서비스 호출 (메일 발송, 결제 등) | 실제로 나간다                 |
| 파일·브랜치 삭제, `git reset --hard`  | 되돌릴 수 없다                |
| 의존성 대량 추가·제거                 | 잠금 파일과 CI에 영향이 크다  |

`git commit`은 요청받았을 때 한다. 묻지 않고 자동으로 커밋하지 않는다.

### 워크플로우 규율은 허가와 별개다

"코드를 써도 된다"가 "아무 때나 써도 된다"는 뜻은 아니다. 스킬이 정한 단계 규율은 그대로 지킨다. 이건 권한 문제가 아니라 **방법론이 성립하느냐**의 문제다.

- `/test-scenarios` 단계에서는 코드를 쓰지 않는다 — 시그니처가 흔들린 채로 테스트를 쓰면 Green에서 테스트를 다시 쓰게 된다
- `/tdd-red` 단계에서는 테스트와 stub만 쓴다 — stub에 로직이 들어가면 빨간불이 무의미해진다
- 각 스킬의 `[GATE]`에서는 실제로 멈춘다

즉 **"물어보지 않고 코드를 쓴다"는 되지만, "단계를 건너뛴다"는 안 된다.**

---

## 문서 구조

```
docs/
├── process/     방법론 — 어떻게 하는가
├── features/    입력 — 원본 요구사항
└── result/      산출물 — 확정된 것
    ├── spec-fixed.md    확정 사양 + 용어 정의
    ├── prd/             도메인별 PRD (기술 결정 ADR 포함)
    ├── adr/             3안 비교 원본
    ├── issues.md        이슈 39개 (수직 슬라이스 + AC)
    └── scenarios/       이슈별 시그니처·테스트 시나리오
```

자세한 내용은 `docs/README.md`.

**무엇을 만드는지**는 `docs/result/spec-fixed.md`, **왜 그 구조인지**는 `docs/result/prd/{도메인}.md`의 기술 결정 섹션을 본다. 구현 전에 해당 도메인의 ADR을 먼저 읽는다. 이미 내려진 결정과 어긋나는 코드를 쓰면 나중에 뜯게 된다.

---

## 스킬

`.claude/skills/`에 있다. 저장소에 커밋되므로 `git pull`만 하면 팀원도 같이 쓴다.

| 스킬              | 언제                                               |
| ----------------- | -------------------------------------------------- |
| `feature-planner` | 새 기능 기획 (인터뷰 → PRD/ADR → 이슈 분해)        |
| `test-scenarios`  | 이슈 하나의 시그니처 확정 + 테스트 시나리오 도출   |
| `tdd-red`         | 시나리오를 실패하는 테스트로 옮기기                |
| `tdd-green`       | 최소 구현으로 테스트 통과시키기                    |
| `tdd-refactor`    | 통과 상태를 유지하며 구조 개선                     |
| `security-review` | 커밋 직전 타입·보안 점검                           |
| `e2e-write`       | PRD 사용자 스토리를 Playwright E2E로 (도메인 단위) |
| `create-pr`       | 검증 통과 후 push + PR 생성                        |
| `tdd-loop`        | 위 7단계를 이슈 하나에 대해 순서대로 (컨테이너)    |
| `tdd-auto-loop`   | 같은 단계를 **묻지 않고** 반반 2교대로 완주 (자율) |

에이전트는 `.claude/agents/`에 있다.

| 에이전트      | 언제                              |
| ------------- | --------------------------------- |
| `ac-verifier` | Green 후 AC 충족 여부를 독립 검증 |

---

## TDD 이슈 사이클

새 이슈를 작업할 때 다음 순서를 따른다.

| #   | 명령                       | 하는 일                                       | 종류  |
| --- | -------------------------- | --------------------------------------------- | ----- |
| 1   | `/test-scenarios N`        | 시그니처 확정 + 시나리오 도출                 | skill |
| 2   | `/tdd-red N`               | 실패 테스트 + stub 작성, 빨간불 확인          | skill |
| 3   | `/tdd-green N`             | 최소 구현, 테스트 전체 통과                   | skill |
| 4   | `@ac-verifier N`           | AC 충족 독립 검증 — **테스트 통과 ≠ AC 충족** | agent |
| 5   | `/tdd-refactor N`          | 구조 개선, 깨지면 즉시 롤백                   | skill |
| 6   | `/security-review N`       | 타입·보안 점검 후 3분류                       | skill |
| 7   | `/create-pr`               | 검증 후 push + PR 생성                        | skill |
| 8   | squash merge → 이슈 클로즈 |                                               |       |

`/tdd-loop N` 하나로 1~7을 순서대로 돌 수도 있다. 컨테이너는 **순서와 사전 점검만** 책임지고, 각 단계의 승인 게이트는 그대로 살아 있다.

`/tdd-auto-loop N`은 같은 7단계를 **묻지 않고 완주**한다. 이 스킬을 부르는 행위가 브랜치·커밋·push·PR 생성에 대한 승인이며, 그래서 **사용자가 명시적으로 부를 때만** 동작한다. "이슈 N 해줘"에는 `/tdd-loop`을 쓴다.

자율 루프가 나누는 단위는 **단계가 아니라 이슈 목록**이다. 이슈 하나는 한 agent가 시나리오부터 PR까지 통째로 만들고, A가 앞 절반 19개를 순서대로 다 끝낸 뒤 B가 뒤 18개를 이어받는다. 순서와 분담은 `docs/result/build-order.md`에 있다.

**재시도 한도가 있다. agent가 죽으면 한 번만 다시 띄우고, 두 번째로 죽으면 사람에게 넘긴다. 이슈 하나가 90분을 넘겨도 멈추고 넘긴다.** 한도가 없어 이슈 #3 하나에 agent를 네 번 다시 띄운 적이 있다.

**각 단계에는 사람의 승인 게이트가 있다. 자동으로 다음 단계로 넘어가지 않는다.**

이건 권한 문제가 아니다. 앞 단계가 틀린 채로 다음으로 넘어가면 **잘못된 시그니처 → 잘못된 시나리오 → 잘못된 테스트**로 오류가 겹쳐 쌓인다. 게이트는 그 전파를 끊기 위해 있다.

4번을 건너뛰지 않는다. Green은 **있는 테스트만** 통과시키므로, AC 하나가 통째로 빠져 있으면 초록불인 채로 이슈가 안 끝난 상태가 된다. 그걸 잡는 게 `ac-verifier`다.

### 브랜치

`docs/result/roadmap-and-split.md` §5의 `feat/{domain}/...` 규칙에 이슈 번호를 붙여 쓴다.

```
feat/{domain}/issue-{N}
예) feat/auth-member/issue-1
```

**이슈 의존성이 있으면 `main`이 아니라 선행 이슈 브랜치에서 분기한다.** 예를 들어 #12는 #11과 #28을 선행으로 두므로, 둘이 아직 머지 전이면 그 브랜치 위에서 시작한다. `main`에서 시작하면 선행 이슈의 결과물이 없어 작업이 막힌다.

선행 관계는 이슈 본문의 **선행** 필드와 `docs/result/issues.md`에 적혀 있다.

### 커밋

commitlint가 걸려 있으므로 형식을 지킨다. 본문에 `closes #N`을 쓰면 머지될 때 이슈가 자동으로 닫힌다.

```
feat: 이메일 인증 코드 발급과 검증을 구현

closes #1
```

> **현재 예외:** 지금은 하네스를 만드는 중이라 문서·스킬을 `main`에 직접 커밋해 왔다.
> 이슈 구현에 들어가면 위 브랜치 규칙을 따른다.

---

## 문서를 쓸 때

**기술 용어는 처음 등장할 때 `용어(한 줄 설명)` 형태로 쓴다.**

```
zod(런타임에 데이터 모양을 검사하고 TypeScript 타입까지 만들어주는 라이브러리)
멱등성(같은 요청을 두 번 보내도 결과가 한 번 보낸 것과 같은 성질)
```

같은 문서에서 두 번째부터는 그냥 쓴다. 용어 사전은
`.claude/skills/feature-planner/references/project-conventions.md`에 있다.

읽는 사람이 문서를 읽다 말고 검색하러 나가지 않게 하는 것이 목적이다.

**용어는 `spec-fixed.md` §0의 정의를 따른다.** 금지어 목록도 거기 있다.
예: "정산·출금"이 아니라 **환전**, "게시글"이 아니라 **공고**.

---

## 기술 스택

| 영역   | 선택                                                      |
| ------ | --------------------------------------------------------- |
| 언어   | TypeScript (strict)                                       |
| 프론트 | Next.js 16 (App Router) + React 19                        |
| 백엔드 | Nest.js 11                                                |
| DB     | PostgreSQL + Prisma 7 (드라이버 어댑터 방식)              |
| 구조   | pnpm 모노레포 (`apps/web`, `apps/api`, `packages/shared`) |
| 스타일 | 일반 CSS. 토큰은 `globals.css`, 화면별은 `*.module.css`   |
| 테스트 | Vitest / Testcontainers(통합) / Playwright(E2E, 예정)     |
| 메일   | Resend                                                    |
| 결제   | 포트원 테스트 모드                                        |

### 기존 패턴

새 코드는 여기에 맞춘다. 저장소가 자라면 같은 계층의 최신 파일이 우선이다.

| 계층         | 참조                                                                                   |
| ------------ | -------------------------------------------------------------------------------------- |
| API 컨트롤러 | `apps/api/src/health/health.controller.ts`                                             |
| 공유 타입    | `packages/shared/src/health.ts` — zod 스키마 하나가 API 검증과 웹 타입을 함께 책임진다 |
| DB 접근      | `apps/api/src/prisma/prisma.service.ts`                                                |
| CSS          | `apps/web/src/app/globals.css`                                                         |

---

## 명령어

```bash
pnpm dev          # web + api 동시 실행
pnpm test         # 전체 테스트
pnpm typecheck    # 타입 검사
pnpm build        # 전체 빌드
pnpm db:up        # Postgres 컨테이너 기동
```

**빌드가 타입 검사보다 먼저다.** Next 16이 `LayoutProps` 같은 전역 타입을 `next build` 때 생성하므로, 빌드 전에 `tsc`를 돌리면 `apps/web`에서 실패한다.

`pnpm`이 PATH에 없으면 `corepack pnpm ...`으로 실행한다.
